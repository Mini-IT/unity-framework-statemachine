# unity-framework-statemachine

Unity views state machine package for Mini IT Unity framework.

Use it to organize screen/view transitions in projects that need this framework module.

Package: `com.miniit.framework.statemachine`
